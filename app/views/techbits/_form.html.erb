<%= form_with(model: techbit, local: true) do |form| %>
  <% if techbit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(techbit.errors.count, "error") %> prohibited this techbit from being saved:</h2>

      <ul>
        <% techbit.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :bit %>
    <%= form.text_area :bit %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
